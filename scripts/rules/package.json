{
  "name": "eslint-plugin-invoke-parallel-rules",
  "version": "0.0.1",
  "description": "Custom ESLint rules for invoke-parallel.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Isiah Meadows",
  "license": "ISC",
  "peerDependencies": {"eslint": "3"}
}
